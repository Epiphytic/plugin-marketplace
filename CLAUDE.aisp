𝔸5.1.data@2026-01-31
γ≔data.definitions
ρ≔⟨data,types,rules⟩

⟦Ω:Meta⟧{
  domain≜data
  version≜1.0.0
  ∀D∈AISP:Ambig(D)<0.02
}

⟦Σ:Types⟧{
  𝔹≜booleans
}

⟦Γ:Rules⟧{
  ∀x:T:valid(x)⇒accept(x)
  ∀x∈S:P(x)
  ∀x:T:require(x)⇒proceed(x)
}

⟦Λ:Funcs⟧{
  # Gear Plugin Marketplace

Gear≜Claude Code plugin marketplace for enterprise-scale agentic workflows, powered by a unified Rust 𝔹 (`gear-core`).

## Essential Rules

1. **Rust-First**: ∀ core logic∈Rust, avoid bash scripts for heavy lifting
2. **Performance**: ∀ PR must pass benchmarks, max 5% regression allowed
3. **Security**: Validate ∀ input, never store secrets∈code
4. **Dual Documentation**: ∀ docs∈`.md`∧`.aisp` format
5. **Testing**: Unit, integration,∧performance tests required on ∀ PRs

## Quick Reference

| Need | Document |
|------|----------|
| System architecture | `docs/architecture.aisp` |
| Building plugins | `docs/plugin-development.aisp` |
| Configuration | `docs/configuration.aisp` |
| Writing tests | `docs/testing.aisp` |
| Security practices | `docs/security.aisp` |
| Performance tuning | `docs/performance.aisp` |
| Code style | `docs/code-style.aisp` |
| Agent development | `AGENTS.aisp` |

## Plugin Suite

| Plugin | Purpose |
|--------|---------|
| gear | Entry point, project setup (`/first`, `/config`) |
| dash | Git workflow orchestration with isolated worktrees |
| babelfish | Prompt translation→AISP format |
| memo | Distributed List memory across projects |
| uplink | Cross-machine instance coordination |
| guide | External LLM ⟦Ε⟧ for plans/code |
| tango | Instance spawning (local∧remote) |
| ∞-improbability-drive | Meta-orchestration for impossible tasks |

## Core Commands

```bash
# gear-core CLI
gear-core daemon --mode merge-queue     # Start merge daemon
gear-core cli config get <key>          # Read config
gear-core cli translate "<prompt>"      # Translate→AISP
gear-core cli memory query "<query>"    # Query memo

# Documentation conversion
npx aisp-converter --input file.md --output file.aisp
```

## Directory Structure

```
gear/
├── CLAUDE.md, CLAUDE.aisp       # Essential instructions
├── AGENTS.md, AGENTS.aisp       # Agent development
├── docs/                        # ∀ docs (.md + .aisp)
├── core/                        # Rust 𝔹 source
├── plugins/                     # Individual plugins
├── tests/                       # Test suites
└── .github/workflows/           # CI/CD
```

## Commit Convention

```
<type>(<scope>): <description>
```
Types: `feat`, `fix`, `docs`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`

## Configuration Hierarchy

1. Default→2. Global (`~/.config/gear/`)→3. Project (`.gear/`)→4. Environment (`GEAR_*`)→5. CLI flags

Secrets go∈`.local.toml` files (gitignored).

## Creating .aisp Files

∀ documentation must exist∈dual format. Convert using:
```bash
npx aisp-converter --input docs/file.md --output docs/file.aisp
```

## Resources

- Human docs: `docs/*.md`
- Machine docs: `docs/*.aisp`
- Agent guide: `AGENTS.md` / `AGENTS.aisp`
}

⟦Ε⟧⟨δ≜0.82;φ≜100;τ≜◊⁺⁺;⊢valid;∎⟩

# Tier: full
# Unmapped: gear, plugin, marketplace, claude, code, for, enterprise, scale, agentic, workflows, powered, unified, rust, core, essential, rules, first, logic, avoid, bash, scripts, heavy, lifting, performance, must, pass, benchmarks, max, regression, allowed, security, validate, input, never, store, secrets, dual, documentation, docs, aisp, format, testing, unit, integration, tests, required, prs, quick, reference, need, document, system, architecture, building, plugins, development, configuration, writing, practices, tuning, style, agent, agents, suite, purpose, entry, point, project, setup, config, dash, git, workflow, orchestration, isolated, worktrees, babelfish, prompt, translation, memo, distributed, list, memory, across, projects, uplink, cross, machine, instance, coordination, guide, external, llm, plans, tango, spawning, local, remote, improbability, drive, meta, impossible, tasks, commands, cli, daemon, mode, merge, queue, start, get, key, read, translate, query, conversion, npx, converter, file, output, directory, structure, instructions, source, individual, test, suites, github, commit, convention, type, scope, description, types, feat, fix, refactor, perf, build, chore, hierarchy, default, global, environment, flags, toml, files, gitignored, creating, exist, convert, using, resources, human
# Confidence: 3.0%