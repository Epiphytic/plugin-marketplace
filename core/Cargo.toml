[package]
name = "gear-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.90"
authors = ["epiphytic"]
description = "Core library for Gear plugin marketplace - AISP conversion with Claude SDK fallback"
license = "MIT"
repository = "https://github.com/epiphytic/gear"

[lib]
name = "gear_core"
path = "src/lib.rs"

[[bin]]
name = "gear-core"
path = "src/main.rs"

[dependencies]
# AISP conversion (rosetta-aisp provides core conversion, aisp provides validation)
rosetta-aisp = { git = "https://github.com/epiphytic/rosetta-aisp.git" }
aisp = { version = "0.1", features = ["serde"] }

# Claude SDK for LLM fallback
claude-agent-sdk-rs = "0.6"

# Async runtime
tokio = { version = "1.49", features = ["full", "signal"] }
futures = "0.3"
async-trait = "0.1"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# CLI
clap = { version = "4.5", features = ["derive", "env"] }

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Utilities
uuid = { version = "1.6", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

# Configuration
directories = "5.0"

[dev-dependencies]
tempfile = "3.9"
tokio-test = "0.4"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "conversion"
harness = false

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"

[features]
default = ["cli"]
cli = []
