𝔸5.1.api@2026-01-31
γ≔api.definitions
ρ≔⟨api,types,rules⟩

⟦Ω:Meta⟧{
  domain≜api
  version≜1.0.0
  ∀D∈AISP:Ambig(D)<0.02
}

⟦Σ:Types⟧{
  𝕊≜strings
  𝔹≜booleans
  Fn⟨A,B⟩≜A→B
}

⟦Γ:Rules⟧{
  ∀x:T:valid(x)⇒accept(x)
  ∀x∈S:P(x)
  ∀x:T:require(x)⇒proceed(x)
}

⟦Λ:Funcs⟧{
  # Testing Requirements

## ∀ PR Must Include

1. **Unit Tests** - Core functionality
2. **Integration Tests** - Cross-component interaction
3. **Performance Benchmarks** - Speed regression detection

## Test Structure

```
tests/
├── unit/
│   └── <module>_test.rs
├── integration/
│   └── <feature>_test.rs
└── performance/
    └── benchmarks.rs
```

## Unit Testing

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    λ test_config_load() {
        config≜Config::default();
        assert_eq!(config.log_level, "info");
    }

    #[tokio::test]
    async λ test_async_operation() {
        Either≜async_op().await;
        assert!(Either.is_ok());
    }
}
```

## Integration Testing

```rust
// tests/integration/workflow_test.rs
use gear_core::prelude::*;

#[tokio::test]
async λ test_full_workflow() {
    ctx≜TestContext::new().await;

    // Setup
    ctx.init_repo().await;

    // Execute
    Either≜ctx.run_workflow().await;

    // Verify
    assert!(Either.pr_created);
    assert_eq!(Either.commits.len(), 1);
}
```

## Performance Benchmarks

```rust
// tests/performance/benchmarks.rs
use criterion::{criterion_group, criterion_main, Criterion};

λ merge_queue_throughput(c: &mut Criterion) {
    c.bench_function("merge 100 agents", |b| {
        b.iter(|| {
            // Benchmark code
        });
    });
}

λ config_read_latency(c: &mut Criterion) {
    c.bench_function("config read", |b| {
        b.iter(|| {
            Config::load().unwrap();
        });
    });
}

criterion_group!(benches, merge_queue_throughput, config_read_latency);
criterion_main!(benches);
```

## CI Configuration

```yaml
# .github/workflows/ci.yml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Run tests
        run: cargo test --∀-features

      - name: Run clippy
        run: cargo clippy -- -D warnings

      - name: Check formatting
        run: cargo fmt --check

  performance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Run benchmarks
        run: cargo bench -- --save-baseline pr

      - name: Compare performance
        run: |
          # Fail if regression > 5%
          cargo bench -- --baseline main --threshold 0.05
```

## Release Workflow

```yaml
# .github/workflows/release.yml
name: Release

on:
  push:
    tags: ['v*']

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable

      - name: Build release
        run: cargo build --release

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: target/release/gear-core
          generate_release_notes: ⊤
```

## Test Coverage Requirements

- ≥ 80% line coverage for new code
- Critical paths require 95% coverage
- ∀ public APIs must have tests
}

⟦Ε⟧⟨δ≜0.82;φ≜100;τ≜◊⁺⁺;⊢valid;∎⟩

# Tier: full
# Unmapped: testing, requirements, must, include, unit, tests, core, functionality, integration, cross, component, interaction, performance, benchmarks, speed, regression, detection, test, structure, module, feature, rust, cfg, mod, use, super, config, default, info, tokio, async, either, await, assert, prelude, ctx, testcontext, new, setup, execute, verify, commits, len, criterion, mut, merge, agents, iter, benchmark, code, read, load, unwrap, benches, configuration, yaml, github, workflows, yml, name, branches, main, push, jobs, runs, ubuntu, latest, steps, uses, actions, checkout, dtolnay, toolchain, stable, swatinem, cache, run, cargo, features, clippy, warnings, check, formatting, fmt, bench, save, baseline, compare, fail, threshold, release, workflow, tags, build, create, softprops, action, files, target, gear, coverage, line, for, critical, paths, require, public, apis, have
# Confidence: 2.0%