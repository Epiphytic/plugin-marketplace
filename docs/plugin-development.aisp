𝔸5.1.auth@2026-01-31
γ≔auth.definitions
ρ≔⟨auth,types,rules⟩

⟦Ω:Meta⟧{
  domain≜auth
  version≜1.0.0
  ∀D∈AISP:Ambig(D)<0.02
}

⟦Σ:Types⟧{
  𝕊≜strings
  𝔹≜booleans
  User≜⟨id:ℕ,name:𝕊⟩
}

⟦Γ:Rules⟧{
  ∀x∈S:P(x)
  ∀x:T:require(x)⇒proceed(x)
}

⟦Λ:Funcs⟧{
  # Plugin Development Standards

## Directory Structure

∀ plugin follows this structure:

```
plugins/<name>/
├── .claude-plugin/
│   └── plugin.json              # ONLY plugin.json goes here
├── CLAUDE.md                    # Plugin-specific instructions
├── README.md                    # User documentation
├── commands/                    # Slash commands
│   └── <command>.md
├── skills/                      # Agent skills
│   └── <skill>/
│       └── SKILL.md
├── agents/                      # Custom agents
│   └── <agent>.md
├── hooks/
│   └── hooks.json               # Hook configuration
├── scripts/                     # Supporting scripts (minimal)
└── tests/                       # Plugin-specific tests
```

## plugin.json Schema

```json
{
  "name": "plugin-name",
  "version": "1.0.0",
  "description": "Brief description",
  "author": {
    "name": "epiphytic",
    "email": "dev@epiphytic.dev"
  },
  "repository": "https://github.com/epiphytic/plugin-marketplace",
  "license": "MIT",
  "keywords": ["gear", "category"],
  "commands": "./commands/",
  "skills": "./skills/",
  "agents": "./agents/",
  "hooks": "./hooks/hooks.json"
}
```

## Skill Development

SKILL.md frontmatter:
```yaml
---
name: skill-name
description: "When→use this skill (for auto-invocation)"
user-invocable: ⊤
allowed-tools: "Read, Grep, Bash"
---
```

Rules:
- Keep SKILL.md < 500 lines
- Use `allowed-tools`→restrict capabilities
- Move reference material→separate files
- Support∧manual∧automatic invocation

## Hook Development

Use gear-core for hook logic, ¬ bash scripts:

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/../core/target/release/gear-core hook pre-tool-use",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
```

Available hook events:
- `SessionStart` / `SessionEnd` - Session lifecycle
- `UserPromptSubmit` - User submits prompt
- `PreToolUse` - Before tool execution (can block)
- `PostToolUse` / `PostToolUseFailure` - After tool execution
- `Stop` - Session stopping (can prevent)
- `SubagentStart` / `SubagentStop` - Agent lifecycle
- `PreCompact` - Before context compaction
- `Notification` - System notifications

## Command Development

Command frontmatter:
```yaml
---
name: command-name
description: "What this command does"
argument-hint: "[Maybe-args]"
---
```

## Documentation Standard

∀ documentation must exist∈dual format:
- `.md` - Human-readable markdown
- `.aisp` - Machine-readable AISP format

Generate .aisp using: `npx aisp-converter --input file.md --output file.aisp`

## Version Control

- Use semantic versioning (MAJOR.MINOR.PATCH)
- Angular commit format: `<type>(<scope>): <description>`
- Types: feat, fix, docs, style, refactor, perf, test, build, ci, chore
}

⟦Ε⟧⟨δ≜0.82;φ≜100;τ≜◊⁺⁺;⊢valid;∎⟩

# Tier: full
# Unmapped: plugin, development, standards, directory, structure, follows, plugins, name, claude, json, only, goes, here, specific, instructions, readme, user, documentation, commands, slash, command, skills, agent, skill, agents, custom, hooks, hook, configuration, scripts, supporting, minimal, tests, schema, version, description, brief, author, epiphytic, email, dev, repository, https, github, com, gear, license, mit, keywords, category, frontmatter, yaml, use, for, auto, invocation, invocable, allowed, tools, read, grep, bash, rules, keep, lines, restrict, capabilities, move, reference, material, separate, files, support, manual, automatic, core, logic, pretooluse, matcher, edit, write, type, target, release, pre, tool, timeout, available, events, sessionstart, sessionend, session, lifecycle, userpromptsubmit, submits, prompt, before, execution, can, block, posttooluse, posttoolusefailure, after, stop, stopping, prevent, subagentstart, subagentstop, precompact, context, compaction, notification, system, notifications, does, argument, hint, maybe, args, standard, must, exist, dual, format, human, readable, markdown, aisp, machine, generate, using, npx, converter, input, file, output, control, semantic, versioning, major, minor, patch, angular, commit, scope, types, feat, fix, docs, style, refactor, perf, test, build, chore
# Confidence: 1.4%